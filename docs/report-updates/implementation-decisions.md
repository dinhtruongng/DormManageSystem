# Implementation Decisions and Report Deltas

Update this file whenever implementation differs from the report or adds demo-specific details.

## Decision log

| Date | Area | Decision | Reason | Impact on report |
|---|---|---|---|---|
| 2026-06-26 | Data flow (§6 DFD) | The "D2 Evidence and Attachment Store" is implemented as Django local media storage for the demo rather than a separate store component. | One-process local demo; gateway abstracts the storage provider so S3-compatible storage can be swapped without code changes. | None — the report already lists local media for demo and S3 for deployment (§7.6). D2 remains conceptually accurate. |
| 2026-06-26 | Database (§6.4, §7.6) | Default database is SQLite; PostgreSQL is selected automatically when `DATABASE_URL` is set. | The report mandates PostgreSQL, but the demo must run with zero external services. Postgres is one environment variable away. | Noted here; the report's PostgreSQL recommendation and constraint list remain the deployment target. |
| 2026-06-26 | Interface (§8.7, §8.8) | The placeholder dashboard and payment-review wireframes were replaced with real screenshots from the running demo. | Report §8.6 requires placeholder replacement once the demo is built. | Source updated: captions and framing text changed from "placeholder" to "implemented screenshot." |
| 2026-06-26 | Interface (§8.6) | Added Appendix A "Implemented Screens" with login, assignment recommendation, payment upload, and check-out screenshots. | Report §8.6 lists exactly these four screens plus dashboard and payment review for screenshot replacement. | Appendix added to `main.tex`. |
| 2026-06-26 | Identity (§8.1) | `UserProfile.role` is a text field on a one-to-one profile model, plus a `role_required` decorator. Django auth Groups are not used to avoid double source of truth. | Keeps role ownership in one table for the demo. Functionally equivalent to the report's "role groups" intent. | None — permission behavior matches the traceability targets (FR-01, NFR-01). |
| 2026-06-27 | Resident import (FR-02, §9.2) | Implemented `residents.services.import_students` (CSV upload) with an admin upload view at `/residents/import/` and an "Import CSV" button on the student list. | FR-02 is Must and names "import"; the report §9.2 specifies `import_students(csv_file)`. Previously only search/view existed. | None — the report already documents CSV student imports (§8.10, §9.2); implementation now matches. |
| 2026-06-27 | Student create API (§8.7) | Added `POST /api/v1/students/create` (Admin-only) and `residents.api.student_create`. | The §8.7 API table lists this endpoint but it had no route before. | None — endpoint table now fully implemented. |
| 2026-06-27 | Room inventory service (§9.4) | Added `rooms.services` with `create_room`, `sync_beds`, `set_bed_status` (validated bed-lifecycle transitions + audit), matching the §9.1 service map. | The report names these methods; the rooms app was previously read-only. | None — §9.4 service method map is now realized in code. |
| 2026-06-27 | Recurring rent (§9.7) | Extracted `billing.services.generate_recurring_rent(period_label)`; `/api/v1/invoices/batches` now calls it instead of inline logic. | §9.1/§9.7 name this service method; it was previously inline in the API handler. | None — service map matches. |
| 2026-06-27 | Report CSV export (FR-12, §9.10, §8.10) | Added `occupancy_export` and `finance_export` CSV download views at `/reports/{occupancy,finance}/export/` with "Export CSV" buttons, plus `finance_report_rows`. | The report promises "downloadable summaries" / "CSV report endpoints" (§6.3, §7.6, §8.10, §9.10); none existed. | None — §8.10 input/output formats and §9.10 reporting module now match. |
| 2026-06-27 | Deployment (§7.8) | Demo is deployed on PythonAnywhere free tier (SQLite, managed WSGI), not the Docker Compose topology in §7.8. | Zero-cost live-URL requirement; Docker topology remains the production target. | Noted here + `docs/DEPLOY_PYTHONANYWHERE.md`. The same code runs locally (SQLite/Postgres via DATABASE_URL) so the §7.8 design still holds for deployment. |
